[project]
name = "my-uv-project"
version = "0.1.0"
description = "My project managed with uv"
authors = [{ name = "Your Name", email = "your@email.com" }]
requires-python = ">=3.8"

dependencies = [
    "huggingface_hub"
]